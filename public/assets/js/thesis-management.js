(() => {
  const root=document.querySelector('#thesisManagementPage'); if(!root)return;
  const items=[...root.querySelectorAll('[data-tm-item]')],search=root.querySelector('[data-tm-search]'),period=root.querySelector('[data-tm-period]'),situation=root.querySelector('[data-tm-situation]'),count=root.querySelector('[data-tm-count]'),empty=root.querySelector('[data-tm-filter-empty]'),pagination=root.querySelector('[data-tm-pagination]'),summary=root.querySelector('[data-tm-summary]'),size=root.querySelector('[data-tm-size]'),pages=root.querySelector('[data-tm-pages]');const state={page:1,size:10};const normalize=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLocaleLowerCase('es');
  const highlight=(node,term)=>{if(!node)return;const source=node.dataset.tmOriginal??node.textContent;node.dataset.tmOriginal=source;const q=normalize(term),i=normalize(source).indexOf(q);if(!q||i<0){node.textContent=source;return;}const mark=document.createElement('mark');mark.textContent=source.slice(i,i+term.length);node.replaceChildren(source.slice(0,i),mark,source.slice(i+term.length));}; const tokens=(page,total)=>{const out=[];for(let n=1;n<=total;n++)if(n===1||n===total||Math.abs(n-page)<=1)out.push(n);else if(out.at(-1)!=='…')out.push('…');return out;};
  const render=()=>{const term=search?.value||'',filtered=items.filter(item=>{const yes=(!term||normalize(item.dataset.search).includes(normalize(term)))&&(!period?.value||item.dataset.period===period.value)&&(!situation?.value||item.dataset.situation===situation.value);item.querySelectorAll('[data-tm-highlight]').forEach(n=>highlight(n,yes?term:''));return yes;});const total=filtered.length,max=Math.max(1,Math.ceil(total/state.size));state.page=Math.min(state.page,max);const first=(state.page-1)*state.size,last=Math.min(first+state.size,total);items.forEach(i=>i.hidden=true);filtered.slice(first,last).forEach(i=>i.hidden=false);if(count)count.textContent=`${total} resultados visibles`;if(empty)empty.hidden=total!==0;if(summary)summary.textContent=total?`Mostrando ${first+1}-${last} de ${total}`:'Mostrando 0 de 0';if(!pagination)return;pagination.hidden=total<=state.size;pages?.replaceChildren();if(!pages)return;const nav=(label,target,disabled)=>{const b=document.createElement('button');b.type='button';b.textContent=label;b.disabled=disabled;b.onclick=()=>{state.page=target;render();};return b;};pages.append(nav('Anterior',state.page-1,state.page===1));tokens(state.page,max).forEach(t=>{if(t==='…'){const s=document.createElement('span');s.textContent=t;pages.append(s);}else{const b=nav(t,t,false);b.className=t===state.page?'is-current':'';pages.append(b);}});pages.append(nav('Siguiente',state.page+1,state.page===max));};[search,period,situation].filter(Boolean).forEach(c=>c.addEventListener('input',()=>{state.page=1;render();}));size?.addEventListener('change',()=>{state.size=+size.value||10;state.page=1;render();});root.querySelectorAll('[data-tm-quick-filter]').forEach(b=>b.onclick=()=>{if(situation){situation.value=b.dataset.tmQuickFilter||'';state.page=1;render();}});render();
  const config=document.querySelector('#thesisTribunalConfig'),overlay=document.querySelector('[data-tribunal-modal]'),dialog=overlay?.querySelector('[data-tribunal-dialog]'),candidateList=overlay?.querySelector('[data-tribunal-candidates]'),selectedList=overlay?.querySelector('[data-tribunal-selected]'),modalSearch=overlay?.querySelector('[data-tribunal-search]'),modalCount=overlay?.querySelector('[data-tribunal-count]'),modalHelp=overlay?.querySelector('[data-tribunal-help]'),modalError=overlay?.querySelector('[data-tribunal-error]'),save=overlay?.querySelector('[data-tribunal-save]'),reason=overlay?.querySelector('[data-tribunal-reason]'),reasonWrap=overlay?.querySelector('[data-tribunal-reason-wrap]'),warning=overlay?.querySelector('[data-tribunal-defense]'),context=overlay?.querySelector('[data-tribunal-context]');let trigger=null,active=null,candidates=[],selected=new Set();
  const message=text=>{if(!modalError)return;modalError.hidden=!text;modalError.textContent=text||'';};const close=()=>{if(!overlay)return;overlay.hidden=true;document.body.classList.remove('tm-modal-open');trigger?.focus();};const draw=()=>{if(!active)return;const term=normalize(modalSearch?.value),limit=selected.size>=5;candidateList.replaceChildren();candidates.filter(p=>!term||normalize(`${p.full_name} ${p.email} ${p.institutional_code}`).includes(term)).forEach(p=>{const label=document.createElement('label');label.className='tm-candidate';const check=document.createElement('input');check.type='checkbox';check.checked=selected.has(+p.id);check.disabled=!check.checked&&limit;check.onchange=()=>{check.checked?selected.add(+p.id):selected.delete(+p.id);draw();};const text=document.createElement('span');text.innerHTML=`<strong></strong><small></small>`;text.querySelector('strong').textContent=p.full_name;text.querySelector('small').textContent=[p.academic_title,p.email].filter(Boolean).join(' · ');label.append(check,text);candidateList.append(label);});selectedList.replaceChildren(...[...selected].map(id=>{const p=candidates.find(x=>+x.id===id);const li=document.createElement('li');li.textContent=p?.full_name||`Docente #${id}`;return li;}));modalCount.textContent=`${selected.size} de 5 miembros seleccionados`;modalHelp.textContent=selected.size<3?'Selecciona al menos 3 docentes para conformar el Tribunal.':selected.size===5?'Se alcanzó el máximo de 5 miembros.':'Composición válida: puedes confirmar el Tribunal.';const defense=active.status==='defense';save.disabled=selected.size<3||selected.size>5||(defense&&reason.value.trim().length<5);};
  const open=async button=>{trigger=button;active={id:+button.dataset.projectId,status:button.dataset.projectStatus,code:button.dataset.projectCode,title:button.dataset.projectTitle};selected=new Set();
    message('');context.textContent=`${active.code} · ${active.title}`;warning.hidden=active.status!=='defense';reasonWrap.hidden=active.status!=='defense';reason.value='';overlay.hidden=false;document.body.classList.add('tm-modal-open');dialog.focus();candidateList.textContent='Cargando docentes disponibles…';try{const r=await fetch(`${config.dataset.candidates}&project_id=${active.id}`,{headers:{Accept:'application/json'}}),json=await r.json();if(!json.success)throw new Error(json.message);candidates=json.data.items||[];const existing=JSON.parse(button.dataset.currentMembers||'[]').map(Number);selected=new Set(candidates.filter(p=>existing.includes(+p.id)).map(p=>+p.id));draw();}catch(e){message(e.message||'No fue posible cargar docentes candidatos.');candidateList.replaceChildren();}};
  root.querySelectorAll('[data-tribunal-manage]').forEach(b=>b.onclick=()=>open(b));overlay?.querySelectorAll('[data-tribunal-close],[data-tribunal-cancel]').forEach(b=>b.onclick=close);overlay?.addEventListener('click',e=>{if(e.target===overlay)close();});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!overlay?.hidden)close();});modalSearch?.addEventListener('input',draw);reason?.addEventListener('input',draw);save?.addEventListener('click',async()=>{if(save.disabled||!active)return;save.disabled=true;message('');const body=new FormData();body.append('_csrf',config.dataset.csrf);body.append('project_id',active.id);body.append('expected_status',active.status);body.append('reason',reason.value);[...selected].forEach(id=>body.append('member_ids[]',id));try{const r=await fetch(config.dataset.save,{method:'POST',body,headers:{Accept:'application/json'}}),json=await r.json();if(!json.success)throw new Error(json.message);window.location.reload();}catch(e){message(e.message||'No fue posible actualizar el Tribunal.');draw();}});
})();

(() => {
  const root=document.querySelector('#thesisManagementPage'), toolbar=root?.querySelector('.tm-toolbar'), search=toolbar?.querySelector('[data-tm-search]'); if(!toolbar||!search)return;
  toolbar.classList.add('ar-tools'); const searchWrap=search.closest('label'); searchWrap?.classList.add('ar-search'); search.setAttribute('role','searchbox');
  const clear=document.createElement('button');clear.type='button';clear.className='tm-search-clear';clear.setAttribute('aria-label','Limpiar búsqueda');clear.innerHTML='<i class="fa-solid fa-xmark" aria-hidden="true"></i>';clear.hidden=!search.value;searchWrap?.append(clear);
  const situation=toolbar.querySelector('[data-tm-situation]');if(situation){const label=situation.closest('label');label?.classList.add('ar-filter-control');const title=label?.querySelector('.sr-only');if(title){title.className='tm-filter-label';title.textContent='Situación';}}
  toolbar.querySelectorAll('.ar-fixed-filter').forEach(control=>{control.classList.add('ar-filter-control');if(!control.querySelector('.tm-filter-label')){const title=document.createElement('span');title.className='tm-filter-label';title.textContent='Período académico';control.prepend(title);}});
  const style=document.createElement('style');style.textContent='#thesisManagementPage .tm-toolbar.ar-tools{display:grid;grid-template-columns:minmax(240px,1fr) repeat(2,minmax(150px,200px));gap:10px;align-items:end;padding:0;border:0}#thesisManagementPage .tm-toolbar .ar-search{height:44px;padding:0 13px;border:1px solid var(--line);border-radius:12px;background:var(--surface-soft);display:flex;align-items:center;gap:10px}#thesisManagementPage .tm-toolbar .ar-search:focus-within{border-color:var(--blue);box-shadow:0 0 0 3px rgba(37,99,235,.09)}#thesisManagementPage .tm-toolbar .ar-search input{width:100%;flex:1;border:0;outline:0;background:transparent;font:inherit}#thesisManagementPage .tm-search-clear{border:0;background:transparent;color:var(--muted);cursor:pointer}#thesisManagementPage .ar-filter-control{display:grid;gap:6px;font-size:.72rem;font-weight:800;letter-spacing:.04em;text-transform:uppercase;color:var(--muted)}#thesisManagementPage .ar-filter-control select,#thesisManagementPage .ar-fixed-filter>div{height:44px;border:1px solid var(--line);border-radius:12px;background:var(--surface-soft);padding:0 13px;font:inherit;color:var(--text)}#thesisManagementPage .ar-fixed-filter>div{display:flex;align-items:center;gap:8px;text-transform:none;letter-spacing:0;font-size:.85rem}@media(max-width:760px){#thesisManagementPage .tm-toolbar.ar-tools{grid-template-columns:1fr}#thesisManagementPage .tm-toolbar.ar-tools>*{min-width:0}}';document.head.append(style);
  const refresh=()=>{clear.hidden=!search.value;};search.addEventListener('input',refresh);clear.addEventListener('click',()=>{search.value='';search.dispatchEvent(new Event('input',{bubbles:true}));search.focus();});
})();

(() => {const c=document.querySelector('#thesisDefenseConfig'),m=document.querySelector('[data-publish-modal]');if(!c||!m)return;const d=m.querySelector('[data-publish-dialog]'),ctx=m.querySelector('[data-publish-context]'),summary=m.querySelector('[data-publish-summary]'),error=m.querySelector('[data-publish-error]'),save=m.querySelector('[data-publish-save]');let trigger=null;const close=()=>{m.hidden=true;document.body.classList.remove('tm-modal-open');trigger?.focus();};document.querySelectorAll('[data-thesis-publish]').forEach(b=>b.onclick=()=>{trigger=b;ctx.textContent=`${b.dataset.projectCode} · ${b.dataset.projectTitle}`;summary.textContent=`Estudiantes: ${b.dataset.students} · Tribunal: ${b.dataset.tribunalCount} miembros`;error.hidden=true;m.hidden=false;document.body.classList.add('tm-modal-open');d.focus();});m.querySelectorAll('[data-publish-close],[data-publish-cancel]').forEach(b=>b.onclick=close);m.addEventListener('click',e=>{if(e.target===m)close();});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!m.hidden)close();});save.onclick=async()=>{if(!trigger||save.disabled)return;save.disabled=true;error.hidden=true;const body=new FormData();body.set('_csrf',c.dataset.csrf||'');body.set('project_id',trigger.dataset.projectId);body.set('expected_status','tribunal_approved');try{const r=await fetch(c.dataset.publish,{method:'POST',body,headers:{Accept:'application/json'}}),j=await r.json();if(!r.ok||!j.success)throw new Error(j.message||'No fue posible publicar el proyecto.');window.location.reload();}catch(x){error.textContent=x.message;error.hidden=false;save.disabled=false;}};})();

(() => {const c=document.querySelector('#thesisDefenseConfig'),m=document.querySelector('[data-result-modal]');if(!c||!m)return;const d=m.querySelector('[data-result-dialog]'),f=m.querySelector('[data-result-form]'),ctx=m.querySelector('[data-result-context]'),members=m.querySelector('[data-result-members]'),effect=m.querySelector('[data-result-effect]'),error=m.querySelector('[data-result-error]'),save=m.querySelector('[data-result-save]');let trigger=null;const close=()=>{m.hidden=true;document.body.classList.remove('tm-modal-open');trigger?.focus();};document.querySelectorAll('[data-tribunal-result]').forEach(b=>b.onclick=()=>{trigger=b;ctx.textContent=`${b.dataset.projectCode} · ${b.dataset.projectTitle}`;members.textContent=`Tribunal: ${b.dataset.tribunalCount} miembros`;f.reset();effect.textContent='';error.hidden=true;m.hidden=false;document.body.classList.add('tm-modal-open');d.focus();});m.querySelectorAll('[data-result-close],[data-result-cancel]').forEach(b=>b.onclick=close);m.addEventListener('click',e=>{if(e.target===m)close();});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!m.hidden)close();});f.result.forEach(r=>r.onchange=()=>{const approved=f.result.value==='approved';effect.textContent=approved?'Al confirmar, el proyecto avanzará al estado Aprobado por el Tribunal.':'El proyecto permanecerá en etapa de defensa. Este resultado quedará registrado en el historial.';save.textContent=approved?'Registrar aprobación':'Registrar resultado';});f.onsubmit=async e=>{e.preventDefault();if(!trigger||save.disabled)return;save.disabled=true;error.hidden=true;const body=new FormData(f);body.set('_csrf',c.dataset.csrf||'');body.set('project_id',trigger.dataset.projectId);body.set('expected_status','defense');try{const r=await fetch(c.dataset.result,{method:'POST',body,headers:{Accept:'application/json'}}),j=await r.json();if(!r.ok||!j.success)throw new Error(j.message||'No fue posible registrar el resultado.');window.location.reload();}catch(x){error.textContent=x.message;error.hidden=false;save.disabled=false;}};})();

(() => {
  const config=document.querySelector('#thesisDefenseConfig'), modal=document.querySelector('[data-defense-info-modal]'); if(!config||!modal)return;
  const dialog=modal.querySelector('[data-defense-info-dialog]'), form=modal.querySelector('[data-defense-info-form]'), context=modal.querySelector('[data-defense-info-context]'), error=modal.querySelector('[data-defense-info-error]'), save=modal.querySelector('[data-defense-info-save]'); let trigger=null;
  const close=()=>{modal.hidden=true;document.body.classList.remove('tm-modal-open');trigger?.focus();};
  document.querySelectorAll('[data-defense-info]').forEach(button=>button.addEventListener('click',()=>{trigger=button;const data=JSON.parse(button.dataset.info||'{}');context.textContent=`${button.dataset.projectCode} · ${button.dataset.projectTitle}`;form.defense_date.value=data.date||'';form.defense_time.value=(data.time||'').slice(0,5);form.location.value=data.location||'';form.modality.value=data.modality||'';error.hidden=true;modal.hidden=false;document.body.classList.add('tm-modal-open');dialog.focus();}));
  modal.querySelectorAll('[data-defense-info-close],[data-defense-info-cancel]').forEach(b=>b.addEventListener('click',close));modal.addEventListener('click',e=>{if(e.target===modal)close();});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!modal.hidden)close();});
  form.addEventListener('submit',async e=>{e.preventDefault();if(!trigger||save.disabled)return;save.disabled=true;error.hidden=true;const body=new FormData(form);body.set('_csrf',config.dataset.csrf||'');body.set('project_id',trigger.dataset.projectId);try{const response=await fetch(config.dataset.info,{method:'POST',body,headers:{Accept:'application/json'}}),result=await response.json();if(!response.ok||!result.success)throw new Error(result.message||'No fue posible guardar la información.');window.location.reload();}catch(exception){error.textContent=exception.message;error.hidden=false;save.disabled=false;}});
})();

(() => {
  const config = document.querySelector('#thesisDefenseConfig');
  const overlay = document.querySelector('[data-defense-modal]');
  if (!config || !overlay) return;

  const dialog = overlay.querySelector('[data-defense-dialog]');
  const context = overlay.querySelector('[data-defense-context]');
  const project = overlay.querySelector('[data-defense-project]');
  const members = overlay.querySelector('[data-defense-members]');
  const error = overlay.querySelector('[data-defense-error]');
  const confirm = overlay.querySelector('[data-defense-confirm]');
  let trigger = null;

  const close = () => {
    overlay.hidden = true;
    document.body.classList.remove('tm-modal-open');
    trigger?.focus();
  };
  const showError = (message = '') => { error.textContent = message; error.hidden = !message; };
  document.querySelectorAll('[data-defense-send]').forEach((button) => button.addEventListener('click', () => {
    trigger = button;
    context.textContent = `${button.dataset.projectCode} · ${button.dataset.projectTitle}`;
    project.textContent = `${button.dataset.projectCode} — ${button.dataset.projectTitle}`;
    const selected = JSON.parse(button.dataset.members || '[]');
    members.replaceChildren(...selected.map((name) => { const item = document.createElement('li'); item.textContent = name; return item; }));
    showError(); overlay.hidden = false; document.body.classList.add('tm-modal-open'); dialog.focus();
  }));
  overlay.querySelectorAll('[data-defense-close],[data-defense-cancel]').forEach((button) => button.addEventListener('click', close));
  overlay.addEventListener('click', (event) => { if (event.target === overlay) close(); });
  document.addEventListener('keydown', (event) => { if (!overlay.hidden && event.key === 'Escape') close(); });
  confirm.addEventListener('click', async () => {
    if (confirm.disabled || !trigger) return;
    confirm.disabled = true; showError();
    const payload = new FormData();
    payload.set('_csrf', config.dataset.csrf || '');
    payload.set('project_id', trigger.dataset.projectId || '');
    payload.set('expected_status', 'approved');
    try {
      const response = await fetch(config.dataset.defense, { method: 'POST', body: payload, headers: { Accept: 'application/json' } });
      const result = await response.json();
      if (!response.ok || !result.success) throw new Error(result.message || 'No fue posible enviar el proyecto a Tribunal.');
      window.location.reload();
    } catch (exception) { showError(exception.message); confirm.disabled = false; }
  });
})();

(() => {
  const style=document.createElement('style');
  style.textContent='#thesisManagementPage .tm-toolbar.ar-tools .ar-search input[data-tm-search]{flex:1 1 0!important;width:100%!important;min-width:0!important;max-width:none!important;color:var(--text)!important}#thesisManagementPage .tm-toolbar.ar-tools .ar-search{min-width:0!important}#thesisManagementPage .tm-search-clear{display:none!important}#thesisManagementPage .ar-fixed-filter>.tm-filter-label{display:none!important}';
  document.head.append(style);
  const search=document.querySelector('[data-tm-search]'),clear=document.querySelector('[data-tm-clear-search]');if(search&&clear){const refresh=()=>{clear.hidden=!search.value;};search.addEventListener('input',refresh);clear.addEventListener('click',()=>{search.value='';search.dispatchEvent(new Event('input',{bubbles:true}));search.focus();});}
})();
