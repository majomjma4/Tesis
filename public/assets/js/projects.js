(() => {
    const root = document.querySelector('[data-projects-page]'); if (!root) return;
    const tabs = [...root.querySelectorAll('[data-project-tab]')];
    const activate = (key, focus = false) => tabs.forEach((tab) => { const active = tab.dataset.projectTab === key; tab.classList.toggle('active', active); tab.setAttribute('aria-selected', String(active)); tab.tabIndex = active ? 0 : -1; root.querySelector(`[data-project-panel="${tab.dataset.projectTab}"]`)?.toggleAttribute('hidden', !active); if (active && focus) tab.focus(); });
    tabs.forEach((tab, index) => tab.addEventListener('keydown', (event) => { if (!['ArrowRight', 'ArrowDown', 'ArrowLeft', 'ArrowUp', 'Home', 'End'].includes(event.key)) return; event.preventDefault(); const next = event.key === 'Home' ? 0 : event.key === 'End' ? tabs.length - 1 : (index + (event.key === 'ArrowRight' || event.key === 'ArrowDown' ? 1 : -1) + tabs.length) % tabs.length; activate(tabs[next].dataset.projectTab, true); }));
    tabs.forEach((tab) => tab.addEventListener('click', () => activate(tab.dataset.projectTab)));
    if (location.hash === '#finished') activate('finished');

    const config = root.querySelector('[data-student-project-publish-config]'); const modal = root.querySelector('[data-project-publish-modal]'); if (!config || !modal) return;
    const ui = (name) => modal.querySelector(`[data-publish-ui-${name}]`);
    const dialog = modal.querySelector('.projects-publish-dialog'); const choice = ui('choice'), manage = ui('manage'), summary = ui('summary'), error = ui('error'), count = ui('count'), previewList = ui('preview'), manageList = ui('files'), excludedBox = ui('excluded'), liveCount = ui('live-count'), empty = ui('empty'), next = ui('next'), confirm = ui('confirm'), back = ui('back'), finalConfirm = ui('final-confirm'), finalText = ui('final-text'), finalAccept = ui('final-accept'), finalCancel = ui('final-cancel');
    const footerCancel = ui('cancel'), closeButton = modal.querySelector('[data-project-publish-close]'); let projectId = 0, preparationId = '', initial = null, plan = null, opener = null, mode = 'choice', busy = false, closing = false, pendingRemoval = null, excluded = new Map();
    const formatCount = (n) => `${n} ${n === 1 ? 'archivo' : 'archivos'}`;
    const formatSize = (value) => { const bytes = Number(value || 0); if (!bytes) return ''; if (bytes < 1024) return `${bytes} B`; const units = ['KB', 'MB', 'GB']; let number = bytes / 1024, index = 0; while (number >= 1024 && index < units.length - 1) { number /= 1024; index++; } return `${number.toFixed(number >= 10 ? 0 : 1)} ${units[index]}`; };
    const typeIcon = (extension) => ({ pdf: 'fa-file-pdf', zip: 'fa-file-zipper', docx: 'fa-file-word', xlsx: 'fa-file-excel', pptx: 'fa-file-powerpoint', png: 'fa-file-image', jpg: 'fa-file-image', jpeg: 'fa-file-image', webp: 'fa-file-image' }[String(extension || '').toLowerCase()] || 'fa-file-lines');
    const request = async (requestMode, extra = {}, upload = null) => { const body = new FormData(); body.set('_csrf', config.dataset.csrf || ''); body.set('project_id', String(projectId)); body.set('mode', requestMode); if (preparationId) body.set('preparation_id', preparationId); Object.entries(extra).forEach(([key, value]) => body.set(key, String(value))); if (upload) body.set('file', upload); const response = await fetch(config.dataset.endpoint || '', { method: 'POST', body, headers: { Accept: 'application/json' } }); const payload = await response.json().catch(() => ({ success: false, message: 'No fue posible procesar la respuesta del servidor.' })); if (!response.ok || !payload.success) throw new Error(payload.message || 'No fue posible procesar la publicación.'); return payload.data || {}; };
    const setBusy = (value, label = '') => { busy = value; modal.classList.toggle('is-busy', value); [...modal.querySelectorAll('button,input')].forEach((control) => { control.disabled = value || (control === next && Number(plan?.file_count || 0) < 1); }); if (label) confirm.innerHTML = `<i class="fa-solid fa-spinner fa-spin" aria-hidden="true"></i>${label}`; };
    const showError = (message = '') => { error.textContent = message; error.hidden = !message; };
    const contextNote = (() => { const node = document.createElement('p'); node.className = 'projects-publish-context-note'; node.hidden = true; node.setAttribute('role', 'status'); manage?.append(node); return node; })();
    const showContext = (kind = '') => { const messages = { add: '<strong>Archivo sin revisión académica</strong><span>Este archivo se añadirá directamente a la publicación final y no ha pasado por una nueva revisión del tutor. Verifica que corresponda a la versión definitiva.</span>', replace: '<strong>Reemplazo de versión final</strong><span>El nuevo archivo no será enviado nuevamente a revisión. La versión aprobada anterior permanecerá guardada en el historial.</span>', remove: '<strong>Quitar de la publicación</strong><span>Este archivo no formará parte de la publicación final. Su versión anterior permanecerá disponible en el historial.</span>' }; contextNote.innerHTML = messages[kind] || ''; contextNote.hidden = !messages[kind]; };
    const switchMode = (value) => { mode = value; finalConfirm.hidden = true; choice.hidden = value !== 'choice'; manage.hidden = value !== 'manage'; summary.hidden = value !== 'summary'; next.hidden = value === 'summary'; confirm.hidden = value !== 'summary'; back.hidden = value === 'choice'; modal.classList.toggle('is-wide', value === 'manage'); showError(); if (value !== 'manage') showContext(); if (value === 'manage') { showContext(); renderManage(plan); } if (value === 'summary') renderSummary(plan || initial); };
    const makeFileItem = (file, compact = false) => { const item = document.createElement('li'); item.className = compact ? 'projects-publish-preview-file' : 'projects-publish-file-row'; const icon = document.createElement('i'); icon.className = `fa-solid ${typeIcon(file.extension)}`; icon.setAttribute('aria-hidden', 'true'); const text = document.createElement('span'); const name = document.createElement('strong'); name.textContent = file.name || 'Archivo'; text.append(name); if (!compact) { const meta = document.createElement('small'); const bits = [formatSize(file.size_bytes), file.kind === 'updated' ? 'Actualizado' : file.kind === 'new' ? 'Nuevo' : 'Aprobado'].filter(Boolean); meta.textContent = bits.join(' · '); text.append(meta); } item.append(icon, text); return item; };
    const renderAvailable = (data) => { const files = data?.files || []; count.textContent = formatCount(data?.file_count || 0); previewList.replaceChildren(...files.map((file) => makeFileItem(file, true))); const toggle = ui('toggle-files'); toggle.hidden = files.length <= 4; previewList.classList.toggle('is-collapsed', files.length > 4); toggle.textContent = `Ver los ${formatCount(files.length)}`; };
    const renderExcluded = () => { if (!excluded.size) { excludedBox.hidden = true; excludedBox.replaceChildren(); return; } excludedBox.hidden = false; const toggle = document.createElement('button'); toggle.type = 'button'; toggle.className = 'projects-publish-text-action'; toggle.textContent = `${formatCount(excluded.size)} excluidos · Ver`; const list = document.createElement('div'); list.hidden = true; for (const file of excluded.values()) { const row = document.createElement('div'); row.textContent = file.name || 'Archivo'; const restore = document.createElement('button'); restore.type = 'button'; restore.textContent = 'Restaurar a publicación'; restore.addEventListener('click', async () => run(async () => { plan = await request('prepare-include', { file_id: file.file_id, included: 'true' }); excluded.delete(file.file_id); renderManage(plan); })); row.append(restore); list.append(row); } toggle.addEventListener('click', () => { list.hidden = !list.hidden; toggle.textContent = `${formatCount(excluded.size)} excluidos · ${list.hidden ? 'Ver' : 'Ocultar'}`; }); excludedBox.replaceChildren(toggle, list); };
    const positionMenu = (button, menu) => { const rect = button.getBoundingClientRect(); menu.style.left = `${Math.max(8, Math.min(rect.right - 158, window.innerWidth - 166))}px`; const below = rect.bottom + 6; const height = Math.min(menu.scrollHeight || 90, window.innerHeight - 16); menu.style.top = `${below + height <= window.innerHeight ? below : Math.max(8, rect.top - height - 6)}px`; };
    const closeFloatingMenus = () => { document.querySelectorAll('.projects-publish-file-menu-list.is-floating').forEach((menu) => { menu.hidden = true; menu.classList.remove('is-floating'); if (menu._origin?.isConnected) menu._origin.appendChild(menu); menu._origin = null; }); document.querySelectorAll('.projects-publish-file-menu[aria-expanded="true"]').forEach((button) => button.setAttribute('aria-expanded', 'false')); };
    const renderManage = (data) => { if (!data) return; closeFloatingMenus(); plan = data; manageList.replaceChildren(); for (const file of data.files || []) { const row = makeFileItem(file); const badge = document.createElement('em'); badge.className = `projects-publish-file-badge is-${file.kind || 'current'}`; badge.textContent = file.kind === 'updated' ? 'Actualizado' : file.kind === 'new' ? 'Nuevo' : 'Aprobado'; row.append(badge); const actions = document.createElement('div'); actions.className = 'projects-publish-file-actions'; const menu = document.createElement('button'); menu.type = 'button'; menu.className = 'projects-publish-file-menu'; menu.setAttribute('aria-label', `Acciones para ${file.name || 'archivo'}`); menu.setAttribute('aria-expanded', 'false'); menu.innerHTML = '<i class="fa-solid fa-ellipsis" aria-hidden="true"></i>'; const menuList = document.createElement('div'); menuList.className = 'projects-publish-file-menu-list'; menuList.hidden = true; menu.addEventListener('click', (event) => { event.stopPropagation(); const opening = menuList.hidden; closeFloatingMenus(); if (opening) { menuList.hidden = false; menuList.classList.add('is-floating'); menuList._origin = actions; document.body.appendChild(menuList); positionMenu(menu, menuList); } menu.setAttribute('aria-expanded', String(opening)); });
            if (file.file_id) { const picker = document.createElement('input'); picker.type = 'file'; picker.hidden = true; picker.addEventListener('cancel', () => showContext('')); picker.addEventListener('change', async () => { const uploaded = picker.files?.[0]; if (!uploaded) { showContext(''); return; } showContext('replace'); closeFloatingMenus(); await run(async () => { plan = await request('prepare-replace', { file_id: file.file_id }, uploaded); renderManage(plan); }); }); const replace = document.createElement('button'); replace.type = 'button'; replace.textContent = 'Reemplazar'; replace.addEventListener('click', () => { showContext('replace'); closeFloatingMenus(); picker.click(); }); const remove = document.createElement('button'); remove.type = 'button'; remove.textContent = 'Quitar de publicación'; remove.addEventListener('click', () => { showContext('remove'); closeFloatingMenus(); askRemoval(file); }); menuList.append(replace, remove, picker); } else { const remove = document.createElement('button'); remove.type = 'button'; remove.textContent = 'Quitar'; remove.addEventListener('click', () => { showContext('remove'); closeFloatingMenus(); askRemoval(file); }); menuList.append(remove); } actions.append(menu, menuList); row.append(actions); manageList.append(row); }
        const total = Number(data.file_count || 0); liveCount.textContent = `${formatCount(total)} se publicarán`; empty.hidden = total > 0; next.disabled = total < 1; renderExcluded(); };
    const renderSummary = (data) => { const files = data?.files || []; const total = Number(data?.file_count || 0); ui('summary-count').textContent = `Se publicarán ${formatCount(total)} en el Repositorio Académico.`; const list = ui('summary-files'); list.replaceChildren(...files.map((file) => makeFileItem(file, true))); list.hidden = true; const toggle = ui('summary-toggle'); toggle.textContent = `Ver archivos incluidos (${total})`; toggle.hidden = !files.length; toggle.onclick = () => { list.hidden = !list.hidden; toggle.textContent = list.hidden ? `Ver archivos incluidos (${total})` : 'Ocultar archivos incluidos'; }; confirm.disabled = total < 1; };
    const close = async (discard = true) => { if (busy || closing || modal.hidden) return; closing = true; closeFloatingMenus(); try { if (discard && preparationId && projectId) { try { await request('prepare-cancel'); } catch (_) {} } modal.hidden = true; document.body.classList.remove('projects-publish-open'); document.body.style.overflow = ''; projectId = 0; preparationId = ''; initial = null; plan = null; excluded.clear(); pendingRemoval = null; finalConfirm.hidden = true; showError(); showContext(); if (opener?.isConnected) opener.focus(); } finally { closing = false; } };
    const run = async (operation, failure = 'No fue posible actualizar los archivos.') => { if (busy) return; setBusy(true); showError(); try { await operation(); } catch (exception) { showError(exception.message || failure); if (mode === 'summary') { confirm.innerHTML = 'Publicar proyecto'; finalAccept.innerHTML = 'Sí, publicar'; } } finally { setBusy(false); } };
    const askRemoval = (file) => { pendingRemoval = file; ui('remove-confirm').hidden = false; ui('remove-confirm').querySelector('button[data-publish-ui-remove-confirm]')?.focus(); };
    root.querySelectorAll('[data-project-publish]').forEach((button) => button.addEventListener('click', () => run(async () => { opener = button; projectId = Number(button.dataset.projectId || 0); if (!projectId) return; initial = await request('preview'); plan = initial; renderAvailable(initial); switchMode('choice'); modal.hidden = false; document.body.classList.add('projects-publish-open'); document.body.style.overflow = 'hidden'; dialog?.focus(); }, 'No fue posible preparar la publicación.')));
    ui('toggle-files')?.addEventListener('click', () => { previewList.classList.toggle('is-collapsed'); ui('toggle-files').textContent = previewList.classList.contains('is-collapsed') ? `Ver los ${formatCount(initial?.file_count || 0)}` : 'Ocultar lista'; });
    ui('keep')?.addEventListener('click', () => { ui('keep').classList.add('is-selected'); ui('update').classList.remove('is-selected'); ui('keep').setAttribute('aria-pressed', 'true'); ui('update').setAttribute('aria-pressed', 'false'); plan = null; switchMode('summary'); });
    ui('update')?.addEventListener('click', () => run(async () => { const data = await request('prepare'); preparationId = data.preparation_id || ''; if (!preparationId) throw new Error('No fue posible iniciar la preparación de archivos.'); plan = data; ui('update').classList.add('is-selected'); ui('keep').classList.remove('is-selected'); ui('update').setAttribute('aria-pressed', 'true'); ui('keep').setAttribute('aria-pressed', 'false'); switchMode('manage'); }));
    ui('add')?.addEventListener('click', () => run(async () => { const input = ui('add-file'); const selected = [...(input?.files || [])]; if (!selected.length) throw new Error('Selecciona al menos un archivo para agregar.'); showContext('add'); for (const uploaded of selected) plan = await request('prepare-add', {}, uploaded); input.value = ''; renderManage(plan); showContext('add'); }));
    ui('next')?.addEventListener('click', () => { if (mode === 'choice') { plan = null; switchMode('summary'); } else if (mode === 'manage' && Number(plan?.file_count || 0) > 0) switchMode('summary'); });
    back?.addEventListener('click', () => switchMode(preparationId ? 'manage' : 'choice'));
    ui('remove-cancel')?.addEventListener('click', () => { pendingRemoval = null; ui('remove-confirm').hidden = true; });
    modal.querySelector('button[data-publish-ui-remove-confirm]')?.addEventListener('click', () => run(async () => { if (!pendingRemoval) return; const file = pendingRemoval; if (file.file_id) { plan = await request('prepare-include', { file_id: file.file_id, included: 'false' }); excluded.set(file.file_id, file); } else plan = await request('prepare-remove-add', { file_key: String(file.key || '').replace('new:', '') }); pendingRemoval = null; ui('remove-confirm').hidden = true; renderManage(plan); }));
    footerCancel?.addEventListener('click', () => close(true)); closeButton?.addEventListener('click', () => close(true)); modal.addEventListener('click', (event) => { if (event.target === modal) close(true); }); document.addEventListener('click', (event) => { if (!event.target.closest('.projects-publish-file-menu, .projects-publish-file-menu-list')) closeFloatingMenus(); });
    confirm?.addEventListener('click', () => { const total = Number((plan || initial)?.file_count || 0); finalText.textContent = `Se publicarán ${formatCount(total)} en el Repositorio Académico.`; finalConfirm.hidden = false; finalAccept.focus(); });
    finalCancel?.addEventListener('click', () => { finalConfirm.hidden = true; confirm.focus(); });
    finalAccept?.addEventListener('click', () => run(async () => { finalAccept.disabled = true; finalAccept.innerHTML = '<i class="fa-solid fa-spinner fa-spin" aria-hidden="true"></i> Publicando…'; await request('publish'); sessionStorage.setItem('student-project-published-notice', '1'); setBusy(false); await close(false); location.assign(`${location.pathname}${location.search}#finished`); }, 'No fue posible publicar el proyecto.'));
    document.addEventListener('keydown', (event) => { if (event.key === 'Escape' && !modal.hidden && !busy) { const floating = modal.querySelector('.projects-publish-file-menu-list.is-floating') || document.querySelector('.projects-publish-file-menu-list.is-floating'); if (floating) { event.preventDefault(); closeFloatingMenus(); return; } if (!finalConfirm.hidden) { finalConfirm.hidden = true; confirm.focus(); } else close(true); } if (event.key !== 'Tab' || modal.hidden) return; const focusable = [...modal.querySelectorAll('button:not([disabled]):not([hidden]),input:not([disabled]):not([hidden])')].filter((element) => element.offsetParent !== null); if (!focusable.length) return; const first = focusable[0], last = focusable.at(-1); if (event.shiftKey && document.activeElement === first) { event.preventDefault(); last.focus(); } else if (!event.shiftKey && document.activeElement === last) { event.preventDefault(); first.focus(); } });
    if (sessionStorage.getItem('student-project-published-notice') === '1') { sessionStorage.removeItem('student-project-published-notice'); const toast = document.createElement('div'); toast.className = 'projects-publish-toast'; toast.setAttribute('role', 'status'); toast.innerHTML = '<i class="fa-solid fa-circle-check" aria-hidden="true"></i> Proyecto publicado correctamente.'; document.body.append(toast); setTimeout(() => toast.remove(), 4500); }
})();
